name: user_preferences_repository
description: Dart package which manages the authentication domain.
publish_to: none

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  shared_preferences: ^2.0.13
  models:
    path: ../models
